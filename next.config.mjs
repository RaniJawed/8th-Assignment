/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.sanity.io'], // Allow images from the cdn.sanity.io domain
    },
  };
  
  export default nextConfig;
  